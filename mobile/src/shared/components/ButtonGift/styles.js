/* @flow */

import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  giftButton: {
    width: 40,
    height: 40,
    top: 19,
    left: 10,
    position: 'absolute',
    zIndex: 100
  },
  giftButtonImage: {
    marginTop: 12,
    marginLeft: 10,
    width: 20,
    height: 20,
    color: 'white'
  },
});

export default styles
